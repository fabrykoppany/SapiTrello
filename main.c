#include <stdio.h>

#include "user.h"
#include "register.h"
#include "file.h"

int main() {
    mainMenu();

    /*USER user1 = createNewUser(), user2;

    printf("%s %s %s %s\n", user1.username, user1.passowrd, user1.firstName, user1.secondName);

    char *fileName = getFileName(user1);

    printf("%s\n", fileName);

    writeToFile(user1);

    user2 = readFile("Users/proba.bin");

    printf("%s\n%s\n%s\n%s", user2.username, user2.passowrd, user2.firstName, user2.secondName);*/
    return 0;
}
